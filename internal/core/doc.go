// Package core holds bfeed's domain types, the interfaces its services
// consume (ports), and the services that implement all business logic.
// It imports no adapter package; dependencies point inward.
package core
